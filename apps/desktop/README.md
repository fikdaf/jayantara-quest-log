# JAYANTARA Desktop

Cross-platform desktop shell for Windows, macOS, and Linux.

The desktop application consumes `@jayantara/core` for quest progression and `@jayantara/platform` for persistence. Platform-specific code must remain isolated from curriculum and progression rules.

Planned runtime: Tauri.
