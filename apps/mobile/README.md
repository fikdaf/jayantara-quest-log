# JAYANTARA Mobile

Cross-platform mobile shell for Android and iOS.

The application must consume `@jayantara/core` for quest progression and `@jayantara/platform` for persistence. Native UI should remain a thin adapter around those packages.

Planned runtime: Expo / React Native.
